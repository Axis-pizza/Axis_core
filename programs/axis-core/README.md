# programs/axis-core

On-chain Axis Core program.

This directory will contain the production Solana program for Axis v1 Core.

Do not add legacy Axis Vault or PFDA logic here directly. Legacy contracts are reference-only.
